"use client";

const SearchPage = () => {
  // "tìm kiếm theo các tag có sẵn thì dùng cho client,
  // còn nếu trong không thuộc phạm phi tag thì sẽ lấy từ khoá và chuyển sang server";

  return <div className={"absolute h-screen w-screen bg-gray-800"}></div>;
};

export default SearchPage;
